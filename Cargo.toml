[package]
name = "tiny_app"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
log = { version = "0.4.22"}

[profile.release]
opt-level = "s"
strip = "symbols"
lto = true
codegen-units = 1
